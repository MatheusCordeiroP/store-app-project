import React from 'react';
import ProfileScreen from './profile.screen';

const ProfileController = () => {
  const handlers = {};
  return <ProfileScreen handlers={handlers} />;
};

export default ProfileController;
